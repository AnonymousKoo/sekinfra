"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
const links=[{href:"/outcomes",label:"Outcomes"},{href:"/how-it-works",label:"How It Works"},{href:"/about",label:"About"}];
export function MobileNav(){const [open,setOpen]=useState(false);useEffect(()=>{const close=(event:KeyboardEvent)=>{if(event.key==="Escape")setOpen(false)};document.addEventListener("keydown",close);document.body.style.overflow=open?"hidden":"";return()=>{document.removeEventListener("keydown",close);document.body.style.overflow=""}},[open]);return <div className="md:hidden"><button aria-expanded={open} aria-controls="mobile-navigation" onClick={()=>setOpen(value=>!value)} className="flex min-h-11 min-w-11 items-center justify-center rounded-md border border-[var(--line)] bg-white" aria-label={open?"Close navigation":"Open navigation"}><span className="text-lg" aria-hidden="true">{open?"×":"≡"}</span></button>{open&&<div id="mobile-navigation" className="absolute inset-x-0 top-[73px] z-20 border-b border-[var(--line)] bg-[var(--background)] p-5 shadow-xl"><nav aria-label="Mobile navigation" className="mx-auto flex max-w-[var(--page-width)] flex-col gap-2">{links.map(link=><Link href={link.href} onClick={()=>setOpen(false)} className="rounded-lg px-4 py-4 text-lg font-semibold hover:bg-[var(--surface-muted)]" key={link.href}>{link.label}<span className="float-right text-[var(--brand)]">→</span></Link>)}<Link href="/start" onClick={()=>setOpen(false)} className="mt-3 rounded-[var(--radius-button)] bg-[var(--brand)] px-4 py-4 text-center font-semibold text-white">Start a Diagnostic</Link></nav></div>}</div>}
