"use client";
import{SystemDiagram}from"@/components/visuals/system-diagram";import{usePersonalization}from"@/components/personalization-provider";import{profiles,PRESSURES,type Pressure}from"@/lib/personalization";
export function ProblemSelector(){const{pressure,profile,setPressure}=usePersonalization();const selected=profile||profiles.leads;return <section aria-labelledby="selector-title" className="section-rule bg-[var(--surface-muted)] py-18 sm:py-28"><div className="mx-auto max-w-[var(--page-width)] px-5 lg:px-8"><div className="grid gap-10 lg:grid-cols-[.75fr_1.25fr] lg:gap-16"><div><p className="eyebrow">Interactive diagnostic lens</p><h2 id="selector-title" className="mt-4 max-w-lg text-4xl font-semibold tracking-[-.05em] sm:text-5xl">Start with the pressure point, then follow the path.</h2><p className="mt-5 max-w-xl text-lg leading-8 text-[var(--ink-muted)]">Choose a focus and the site will adapt during this browsing session. Nothing is sent, stored beyond this session, or used to identify you.</p><div className="mt-8 flex flex-wrap gap-2" role="tablist" aria-label="Operational problem areas">{PRESSURES.map(id=>{const item=profiles[id];return <button key={id} role="tab" aria-selected={pressure===id} onClick={()=>setPressure(id as Pressure)} className={`min-h-11 rounded-full border px-4 text-sm font-bold transition ${pressure===id?"border-[var(--brand)] bg-[var(--brand)] text-white shadow-sm":"border-[var(--line)] bg-white hover:border-[var(--brand)] hover:bg-[var(--brand-wash)]"}`}>{item.label}</button>})}</div><p className="mt-7 border-l-2 border-[var(--brand)] pl-4 text-sm leading-6 text-[var(--ink-muted)]">{pressure?"Your focus can be changed or reset at any time.":"Choose a focus to see how the experience adapts."}</p></div><div className="rounded-[var(--radius-card)] border border-[var(--line)] bg-white p-4 shadow-[0_20px_50px_rgba(16,37,31,.06)] sm:p-6"><SystemDiagram variant="selector" activeLabel={selected.label} flow={selected.flow}/><div className="mt-6 grid gap-5 sm:grid-cols-3"><div><p className="eyebrow">Signal</p><p className="mt-2 text-sm leading-6">{selected.recognition}</p></div><div><p className="eyebrow">Consequence</p><p className="mt-2 text-sm leading-6 text-[var(--ink-muted)]">{selected.consequence}</p></div><div><p className="eyebrow">Desired outcome</p><p className="mt-2 text-sm font-semibold leading-6 text-[var(--brand)]">{selected.outcome}</p></div></div></div></div></div></section>}
