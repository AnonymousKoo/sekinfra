{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:sekinfra:schema:contracts:common:identifiers:v1",
  "title": "Sekinfra Slice 1 Foundational Identifiers v1",
  "description": "Provider-neutral, bounded identifier primitives for the Sekinfra Slice 1 contract boundary. Canonical internal UUIDs are distinct from opaque external references.",
  "$defs": {
    "canonicalUuid": {
      "title": "Canonical UUID",
      "description": "Lowercase RFC 4122/9562 textual UUID used for Sekinfra-owned canonical identities.",
      "type": "string",
      "minLength": 36,
      "maxLength": 36,
      "format": "uuid",
      "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
    },
    "tenantId": {
      "title": "Tenant ID",
      "description": "Canonical Sekinfra tenant identity.",
      "$ref": "#/$defs/canonicalUuid"
    },
    "engagementId": {
      "title": "Engagement ID",
      "description": "Canonical Sekinfra engagement identity; never an acquisition or provider account ID.",
      "$ref": "#/$defs/canonicalUuid"
    },
    "commandId": {
      "title": "Command ID",
      "description": "Unique identity for one command attempt.",
      "$ref": "#/$defs/canonicalUuid"
    },
    "correlationId": {
      "title": "Correlation ID",
      "description": "Identity correlating one bounded business interaction.",
      "$ref": "#/$defs/canonicalUuid"
    },
    "causationId": {
      "title": "Causation ID",
      "description": "Identity of the accepted command or event that directly caused another action.",
      "$ref": "#/$defs/canonicalUuid"
    },
    "eventId": {
      "title": "Event ID",
      "description": "Unique identity for one immutable lifecycle event.",
      "$ref": "#/$defs/canonicalUuid"
    },
    "approvalId": {
      "title": "Approval ID",
      "description": "Canonical identity for one attributable human approval record.",
      "$ref": "#/$defs/canonicalUuid"
    },
    "assessmentAccessProposalId": {"$ref": "#/$defs/canonicalUuid"},
    "oiaAssessmentId": {"$ref": "#/$defs/canonicalUuid"},
    "oiaAssessmentPlanId": {"title": "OIA Assessment Plan ID", "description": "Canonical identity for one versioned client-specific OIA assessment plan resource.", "$ref": "#/$defs/canonicalUuid"},
    "oiaInspectionItemId": {"title": "OIA Inspection Item ID", "description": "Canonical identity for one first-class OIA diagnostic inspection item.", "$ref": "#/$defs/canonicalUuid"},
    "oiaEvidenceId": {"$ref": "#/$defs/canonicalUuid"},
    "oiaObservationId": {"$ref": "#/$defs/canonicalUuid"},
    "oiaRootCauseId": {"$ref": "#/$defs/canonicalUuid"},
    "oiaFindingId": {"$ref": "#/$defs/canonicalUuid"},
    "oiaFindingsDeliveryId": {"$ref": "#/$defs/canonicalUuid"},
    "oiaConversionDecisionId": {"title": "OIA Conversion Decision ID", "description": "Canonical identity for one delivery-bound client conversion decision history.", "$ref": "#/$defs/canonicalUuid"},
    "ongoingAgreementAuthorityId": {"title": "Ongoing Agreement Authority ID", "description": "Canonical identity for one versioned Agreement #2 authority history.", "$ref": "#/$defs/canonicalUuid"},
    "ongoingPaymentVerificationId": {"title": "Ongoing Payment Verification ID", "description": "Canonical identity for one bounded ongoing commercial verification.", "$ref": "#/$defs/canonicalUuid"},
    "ongoingAccessGrantId": {"title": "Ongoing Access Grant ID", "description": "Canonical identity for one bounded ongoing access authority, distinct from diagnostic access.", "$ref": "#/$defs/canonicalUuid"},
    "ongoingAccessRevocationVerificationId": {"title": "Ongoing Access Revocation Verification ID", "description": "Canonical identity for immutable verification that external ongoing access is absent.", "$ref": "#/$defs/canonicalUuid"},
    "ongoingOffboardingId": {"title": "Ongoing Offboarding ID", "description": "Canonical identity for one governed ongoing-engagement offboarding history.", "$ref": "#/$defs/canonicalUuid"},
    "assessmentAccessGrantId": {
      "title": "Assessment Access Grant ID",
      "description": "Canonical identity for one temporary diagnostic assessment access authority record.",
      "$ref": "#/$defs/canonicalUuid"
    },
    "diagnosticPaymentVerificationId": {
      "title": "Diagnostic Payment Verification ID",
      "description": "Canonical identity for one diagnostic/OIA payment verification record.",
      "$ref": "#/$defs/canonicalUuid"
    },
    "diagnosticAgreementAuthorityId": {
      "title": "Diagnostic Agreement Authority ID",
      "description": "Canonical identity for one verified diagnostic/OIA agreement-authority record.",
      "$ref": "#/$defs/canonicalUuid"
    },
    "scopeId": {
      "title": "Diagnostic Scope ID",
      "description": "Canonical identity for a diagnostic scope aggregate.",
      "$ref": "#/$defs/canonicalUuid"
    },
    "handoffId": {
      "title": "Acquisition Handoff ID",
      "description": "Canonical Sekinfra identity for an accepted handoff snapshot; provider/source references remain separate.",
      "$ref": "#/$defs/canonicalUuid"
    },
    "opaqueReferenceId": {
      "title": "Opaque Provider-Neutral Reference Identifier",
      "description": "Bounded opaque reference for an external or provider-owned object. It is never a canonical Sekinfra internal identity and carries no credential material.",
      "type": "string",
      "minLength": 3,
      "maxLength": 128,
      "pattern": "^[a-z][a-z0-9]*(?:[._:-][a-z0-9]+)*$"
    },
    "schemaIdentifier": {
      "title": "Schema Identifier",
      "description": "Immutable Sekinfra contract schema URN with an explicit major version.",
      "type": "string",
      "minLength": 24,
      "maxLength": 160,
      "pattern": "^urn:sekinfra:schema:[a-z0-9]+(?:[:_-][a-z0-9]+)*:v[1-9][0-9]*$"
    },
    "idempotencyKey": {
      "title": "Idempotency Key",
      "description": "Caller-generated bounded key for one semantic command request. It must not contain credential material.",
      "type": "string",
      "minLength": 16,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{15,127}$",
      "allOf": [
        {
          "not": {
            "pattern": "(?:[Bb][Ee][Aa][Rr][Ee][Rr]|[Pp][Aa][Ss][Ss][Ww][Oo][Rr][Dd]|[Ss][Ee][Cc][Rr][Ee][Tt]|[Tt][Oo][Kk][Ee][Nn]|[Aa][Pp][Ii][_-]?[Kk][Ee][Yy])"
          }
        }
      ]
    }
  }
}
