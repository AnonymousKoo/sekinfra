{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:sekinfra:schema:contracts:commands:verify-assessment-access-payload:v1",
  "title": "Verify Assessment Access Payload v1",
  "description": "Strict request to verify one authoritative AssessmentAccessGrant. Verification outcome, authority, timestamps, credentials, provider data, evidence, and metadata are server-derived or forbidden.",
  "type": "object",
  "additionalProperties": false,
  "required": ["assessment_access_grant_id"],
  "properties": {
    "assessment_access_grant_id": {"$ref": "urn:sekinfra:schema:contracts:common:identifiers:v1#/$defs/assessmentAccessGrantId"}
  }
}
