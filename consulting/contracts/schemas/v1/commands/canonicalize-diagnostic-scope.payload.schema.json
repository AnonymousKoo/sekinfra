{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:sekinfra:schema:contracts:commands:canonicalize-diagnostic-scope-payload:v1",
  "title": "Canonicalize Diagnostic Scope Payload v1",
  "description": "Strict target and concurrency input for server-side canonicalization of an existing DiagnosticScope. Canonical content and digest are derived solely from authoritative stored state.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "diagnostic_scope_id",
    "scope_version"
  ],
  "properties": {
    "diagnostic_scope_id": {
      "$ref": "urn:sekinfra:schema:contracts:common:identifiers:v1#/$defs/scopeId"
    },
    "scope_version": {
      "$ref": "urn:sekinfra:schema:contracts:domain:diagnostic-scope:v1#/$defs/positiveVersion"
    }
  }
}
